<h2><%= @word.friendly_type_name %></h2>

<p><%= @word.class.friendly_type_description %></p>

<%= error_messages_for :word %>

<% form_tag( :action => 'update', :id => @word.id ) do %>
	<%= render :partial => 'form' %>
	<p><%= submit( 'save' ) %></p>
<% end %>
